'''
Faça um programa que peça ao usuário para digitar um número inteiro,
informe se este número é par ou impar. Caso o usuário não digite
um número inteiro, informe que não é um número inteiro.
'''
try:
    valor = int(input('Digite um valor: '))
    if valor % 2 == 0:
        print(f'O numero é {valor} ele é par')
    else: 
        print(f'O número é {valor} ele é impar')

except:
    print('Digite um número inteiro apenas')